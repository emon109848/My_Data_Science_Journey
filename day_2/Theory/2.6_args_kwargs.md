Perfect 🔥
Here’s your **PRINTABLE `*args` & `**kwargs` CHEAT SHEET** — compact, structured, interview-ready.

---

# 📌 PYTHON `*args` & `**kwargs` — COMPLETE REVISION SHEET

---

## 1️⃣1️⃣ `*args` — Variable Positional Arguments

---

# 🧠 What is `*args`?

* Allows function to accept **any number of positional arguments**
* Stored as a **tuple** inside the function
* Name `args` is conventional (can use any valid name)

---

# 🔹 1️⃣ Syntax

```python
def func(*args):
    print(args)
```

---

# 🔹 2️⃣ Example — Sum Numbers

```python
def add(*args):
    total = 0
    for n in args:
        total += n
    return total

print(add(1, 2, 3, 4))  # 10
```

* Any number of arguments can be passed

---

# 🔹 3️⃣ Unpacking Arguments

```python
nums = [1, 2, 3]
add(*nums)  # same as add(1,2,3)
```

---

# 🔹 4️⃣ Use Cases

✅ Functions with variable number of inputs
✅ Wrappers / decorators
✅ Mathematical operations (sum, product)
✅ Passing list/tuple as multiple arguments

---

# 🎯 Common Mistakes

❌ Using after regular positional arguments incorrectly
❌ Forgetting the `*` when unpacking list/tuple

---

## 1️⃣2️⃣ `**kwargs` — Variable Keyword Arguments

---

# 🧠 What is `**kwargs`?

* Allows function to accept **any number of keyword arguments**
* Stored as a **dictionary** inside the function
* Name `kwargs` is conventional (can use any valid name)

---

# 🔹 1️⃣ Syntax

```python
def func(**kwargs):
    print(kwargs)
```

---

# 🔹 2️⃣ Example — User Info

```python
def user_info(**kwargs):
    for key, val in kwargs.items():
        print(f"{key}: {val}")

user_info(name="Emon", age=22, city="Dhaka")
```

Output:

```
name: Emon
age: 22
city: Dhaka
```

---

# 🔹 3️⃣ Unpacking Dictionary

```python
info = {"name": "Alex", "age": 19}
user_info(**info)
```

---

# 🔹 4️⃣ Mixed Usage

```python
def func(a, b, *args, **kwargs):
    print(a, b)
    print(args)
    print(kwargs)

func(1, 2, 3, 4, x=10, y=20)
```

Output:

```
1 2
(3, 4)
{'x': 10, 'y': 20}
```

* Order: positional → `*args` → keyword → `**kwargs`

---

# 🔹 5️⃣ Use Cases

✅ Functions with optional named arguments
✅ Flexible API design
✅ Wrappers / decorators
✅ Passing dicts as arguments

---

# 🧠 INTERNAL WORKING

* `*args` → tuple
* `**kwargs` → dict
* Can be combined with **regular arguments**
* Allows **packing & unpacking**

```python
def f(a, *args, **kwargs):
    pass
```

---

# 🎯 Common Mistakes

❌ Wrong order of arguments
❌ Forgetting `*` / `**` when unpacking
❌ Using both incorrectly together

---

# 🔥 Interview Questions (`*args` & `**kwargs`)

1. What is the difference between `*args` and `**kwargs`?
2. What data type does `*args` store? What about `**kwargs`?
3. Can `*args` come after a regular argument?
4. Can `**kwargs` come before `*args`?
5. How to unpack a list or dict into `*args` / `**kwargs`?
6. Give a real use case for `*args` and `**kwargs`.
7. What happens if a keyword argument conflicts with a positional argument?
8. Can a function have both `*args` and `**kwargs`?

---

If you want, I can **next create a single “Advanced Python Functional & Args/Iteration Cheat Sheet”** combining:

* `lambda`, `map`, `filter`, `reduce`, `zip`, `enumerate`, `*args`, `**kwargs`
* Examples + interview questions
* Printable single-page style

Do you want me to prepare that next?
